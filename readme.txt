   Write 4 methods to build a calculator:

    add: requires 2 arguments and returns their total.
    subtract: requires 2 arguments and returns their difference.
    multiply: requires 2 arguments and returns their multiplication.
    div: requires 2 arguments and returns their division.

